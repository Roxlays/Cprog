#include "lib.h"

int main(){}


Map* get_map(int height, int weidth)
{
    Map* map = malloc(sizeof(Map));
    //logique height weidth
    return map;
}

Player get_player(Map* map)
{
    //instance Player
    //player position = somewhere on the map
}

void load_bloack(Map* map){}